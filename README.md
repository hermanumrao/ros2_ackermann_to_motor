# ros2_ackermann_to_motor
This packa is to be used in combination with ros2_ackermann_motor_ctrl package. This converts the `ackermann_msgs::msg::AckermannDriveStamped` msgs to std_msgs/msg/String whis is required for my package to convert ackermann commands to hardware motor outputs.
